import { mobileValidator } from "../components/fs-phone-input/index";
export default function (): {
    phone: {
        column: {
            cellRender({ value }: any): JSX.Element | "";
        };
        form: {
            component: {
                name: string;
                vModel: string;
            };
            rules: {
                validator: typeof mobileValidator;
                message: string;
            }[];
        };
    };
};
